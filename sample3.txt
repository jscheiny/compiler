struct A(pub integer: int, boolean: bool) {
    test(value: A): bool {
        return value.boolean;
    }
}

enum B() {
    test(value: A): bool {
        value.test(value);
        return value.boolean;
    }
}

fn test(a: Aasdfa) {
    a.integer;
    a.boolean or false;
}