struct Pair(first: int, second: int, impl cool: bool) {
    .sum(): int -> @first + @second;

    impl .total(): int -> @first + @second;
}

fn test() {
    let p = Pair(3, 4);
    let t: int = p.sum();
    let t2 = p.total();
    let x: Pair => bool = Pair::first;
}