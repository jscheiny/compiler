type T = bool -> bool -> bool;

fn test() {
    accept(a -> b -> (a or b));
    // TODO why does the type inference here do this...
    let x: [int -> int] = a -> -a;
    let x = 3 -> 4;
}

fn accept(value: T) {
    accept2(value(true));
}

fn accept2(value: bool -> bool) {

}