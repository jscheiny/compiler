struct A(pub value: () -> bool) {
}

struct B(private_a: A) {
    get_a(): A -> @private_a;
}

struct C(pub b: B);

type T = C;

fn test(c: T) {
    while c.b.get_a().value() {}

    while A.value(c.b.get_a())() { // TODO need to fix this -> this should be a function

    }
}