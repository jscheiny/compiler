enum X(A, B, C,) {}

interface I {
    get_self(): Self;
}

struct S() {
    pub duplicate(): S {
        let g: Self = S();
    }
}


type Identity[T] = T;

fn test(s: S) {
    let f1: () => S = s.duplicate;
    let f2: S => S = Identity[S]::duplicate;
    let x = (a: bool, b: bool) -> a or b;
    accept(a -> not a);

    let x: X = C(5);
}

fn accept(f: bool => bool) {
    let x: Self = 5;
}