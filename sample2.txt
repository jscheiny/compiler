enum X(A, B, C,) {
    .matc() {}
}

interface I {
    ::get_self(): int;
}

struct S() {
    pub ::duplicate(): S {
        let g: Self = S();
    }

    impl I {
        get_self(): int -> 5;
    }
}


type Identity[T] = T;

fn test(s: S) {
    let f1: () => S = s.duplicate;
    let f2: S => S = Identity[S]::duplicate;
    let x = (a: bool, b: bool) -> a or b;
    accept(a -> not a);

    let x: X = C;
}

fn accept(f: bool => bool) {
    let x = 5;
}