interface Test {
    test(value: int);
    test2(value: bool): int;
}

struct S() {
    impl Test {
        pub test(value: int): int -> value;
    }
}

fn test(test: Test) {
    test.test(3);
    test.test2(false);
    accept(.test2(true));
}

fn accept(getter: Test => int){}