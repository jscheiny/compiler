fn test(value: Neato) {
    type T = bool;
    let e = E.A;
    let x: [char] = e.to_string();
}

type T = [char];

interface ToString {
    to_string(): T;
}

struct Neato(value: int) {
    to_int(param: bool): int -> @value;
    a(): T -> "hello world";

    impl ToString {}
}

enum E(A, B, C) {
    impl ToString;
}