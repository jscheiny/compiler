interface ToString {
    to_string(): [char];
}

struct Neato(value: int) {
    to_int(param: bool): int -> @value;

    impl ToString {}
}

fn test(value: Neato) {
    let x: int = value.value;
    let y: int = value.to_int(true);
    let z: [char] = value.to_string();
}